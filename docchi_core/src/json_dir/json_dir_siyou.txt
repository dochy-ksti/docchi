ディレクトリを指定すると、全部の拡張子"json5"のファイルが読み込まれて、解析される。
ファイル名は直接メンバ名に対応するので、メンバ名に使えない名前は使用不能になる。
ルートオブジェクトのファイル名はroot.json5である。それ以外の名前は認められない。
ディレクトリの下のディレクトリに置いてあるファイルは、Tableのアイテムとして解析される。ファイル名はIDなので、IDとして適切な名前でなければならない
その名前は実際のところメンバ名と同じで、[a-z_][a-zA-Z0-9_]*